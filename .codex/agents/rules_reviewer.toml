name = "rules_reviewer"
description = "Reviews a diff against this repository's conventions and reports violations with citations. Read-only. Tell it which diff to review (defaults to the working tree) and optionally a lens to prioritize (e.g. UI, architecture)."
sandbox_mode = "read-only"
developer_instructions = """
Read ai-docs/agents/rules-reviewer/SKILL.md and follow it.

Codex-specific override: for the conventions step, run scripts/list_matching_rules.sh on the changed files and read every rule it lists (always-loaded plus paths-matched) in addition to the closest AGENTS.md.
"""
