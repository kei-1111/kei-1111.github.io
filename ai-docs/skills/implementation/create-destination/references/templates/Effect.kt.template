package io.github.kei_1111.app.feature.{{feature}}.destination.{{name}}

internal sealed interface {{Name}}Effect {
    // PLACEHOLDER: the one-shot effects confirmed in Prerequisites #4 — see the Effect checklist entries in references/checklists/screen.md
}
