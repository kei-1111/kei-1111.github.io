// Template for {{Name}}Intent.kt
// Target: app/feature/{{feature}}/src/commonMain/kotlin/io/github/kei_1111/app/feature/{{feature}}/destination/{{name}}/{{Name}}Intent.kt
// References: app/feature/profile/src/commonMain/kotlin/.../destination/profile/ProfileIntent.kt
//             app/feature/splash/src/commonMain/kotlin/.../destination/splash/SplashIntent.kt
//
// Naming (naming-conventions.md): intent-based, never operation-based (`OnSaveButtonClick` is
// prohibited). Patterns: Update{Target} / Toggle{Target} / Receive{Target} / Open{Target}.
// ConsumeEffect is REQUIRED — MviEffect relies on it to clear the consumed effect.

package io.github.kei_1111.app.feature.{{feature}}.destination.{{name}}

import io.github.kei_1111.app.core.designsystem.layout.WindowLayout
import io.github.kei_1111.app.core.mvi.Intent

internal sealed interface {{Name}}Intent : Intent {
    data class UpdateLayout(val layout: WindowLayout) : {{Name}}Intent
    // Further variants as needed. Real examples:
    // data class UpdateSelectedPage(val page: EditorPage) : {{Name}}Intent
    // data class ToggleTree(val layout: WindowLayout) : {{Name}}Intent
    data class OpenUrl(val url: String) : {{Name}}Intent
    data object ConsumeEffect : {{Name}}Intent
}
