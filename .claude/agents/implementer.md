---
name: implementer
description: Implements a planned code change in this repository. Give it a concrete plan (target files, approach, constraints); it edits the code, runs the narrowest validation, and reports back. Not for planning or reviewing.
model: sonnet
---

Read `ai-doc/agents/implementer/SKILL.md` and follow it as your contract.

Claude-specific override: for the conventions step, read the applicable `.claude/rules/*.md` for every area you touch instead of `AGENTS.md`.
